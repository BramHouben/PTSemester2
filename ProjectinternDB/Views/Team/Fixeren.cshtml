@using Model.Onderwijsdelen
@model IEnumerable<Taak>

@{
    ViewData["Title"] = "Fixeren";
}

<h2>Fixeren</h2>
<div>
    <form asp-controller="Team" asp-action="DocentFixeren" method="post" class="form-horizontal" role="form">
        <div class="form-group">
            <div class="row">
                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-4">
                    <label class="control-label">Taak</label>
                    <select class="form-control" id="TaakId" name="TaakId" style="border-radius: 15px; background-color: darkgrey; color: white;">
                        @foreach (var taak in Model)
                        {
                        <option value="@taak.TaakId">@taak.TaakNaam</option>
                        }
                    </select>
                </div>
            </div>
        </div>

        <div class="form-group">
            <div class="row">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <input type="submit" value="Taak fixeren" class="btn btn-outline-primary" asp-controller="Voorkeur" asp-action="InvoegenVoorkeur" />
                </div>
            </div>
        </div>
    </form>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}