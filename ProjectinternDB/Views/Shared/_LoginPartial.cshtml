@using Microsoft.AspNetCore.Identity
@*@model ProjectinternDB.Models.MedewerkerViewModel*@
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@if (User.Identity.IsAuthenticated)
{
    //    var user = await UserManager.FindByNameAsync(User.Identity.Name);



    @if (SignInManager.IsSignedIn(User))
    {
        @*<script>
            var element = document.getElementById("navbarColor03");
            element.classList.add("noFlexGrow");
        </script>*@
        //if (User.IsInRole(""))
        //{
  
        //}

        <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutForm" class="rm-inline my-2 my-lg-0">
            <ul class="nav navbar-nav navbar-right">
                <li class="nav-item">
                    <a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage" class="nav-link">Hallo @UserManager.GetUserName(User)</a>
                </li>
                <li class="nav-item">
                    <button type="submit" class="btn btn-link navbar-btn navbar-link btn btn-secondary my-2 my-sm-0">Logout</button>
                </li>
                <li>
                    <a asp-area="Identity" asp-page="/Account/Manage/Index"><img src="~/images/user.png" height="50" alt="User" class="imgNavbar" /></a>
                </li>
            </ul>

        </form>

    }

}
else
{
    <ul class="nav navbar-nav navbar-right">

        <li class="nav-item"><a asp-area="Identity" asp-page="/Account/Register" class="nav-link">Registreren</a></li>
        <li class="nav-item"><a asp-area="Identity" asp-page="/Account/Login" class="nav-link">Login</a></li>
    </ul>
}